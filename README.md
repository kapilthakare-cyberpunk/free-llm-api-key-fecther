# Free LLM API Key Fetcher (MenuBar)

A native macOS MenuBar app to manage free LLM API keys.

## Build & Run
1. Open in Xcode: `open Package.swift`
2. Or build via command line: `swift build`
3. Run: `.build/debug/FreeLLMKeyManager`
